/**
 * This is a placeholder module that replaces the enterprise ui-theming module.
 * It contains stubs for the functionality used by the open-source code.
 */

export const useTheme = () => {
  return {
    theme: 'light',
    setTheme: () => {},
  };
};

export default {
  useTheme,
}; 